scaffold:
  contract_version: 6
  mode: existing
  execution_mode: preview_then_apply

repo:
  shape: unknown
  app_dir: apps
  shared_dir: packages
  docs_entry: docs/current.md
  command_entry: unknown

ai_collaboration:
  language: zh-first
  skill_entry: skills/meta-scaffold/SKILL.md
  default_context:
    - AGENTS.md
    - docs/current.md
    - docs/reference/architecture.md
  preview_before_apply: true
  verify_after_apply: true

notes:
  - 本文件是可选的。
  - 它只记录 AI 需要知道的结构决策。
  - 它不替代真实项目配置。
