---
description: META-SCAFFOLD v6.6 repository governance, project memory, verification and handoff rules
alwaysApply: true
---

# META-SCAFFOLD v6.6

- 先读用户请求、当前目录项目规则和直接相关文件；需要当前状态时再读 `docs/current.md`。
- 小任务直接修改并验证；多文件或结构调整才先说明目标、事实、假设、成功标准和非目标。
- 尊重现有仓库形态；只有真实收益时才新增边界或建议 monorepo。
- 采用 `apps/`/`packages/` 时，共享包不得反向依赖 app；跨 app 走契约或服务接口。
- 高影响操作按项目规则取得授权；已批准计划内明确步骤不重复阻塞。
- 优先运行已有验证入口；失败或未运行如实说明，不 silent fallback。
- 项目记忆按寿命放入 current、ADR、reference、roadmap 或 local plan。
- 结果和交接必须自包含；只有暂停或换会话时才生成 handoff prompt。

完整 skill：`skills/meta-scaffold/SKILL.md`。
